---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.

The only files you may write during this skill are `CONTEXT.md` and files under `docs/adr/`. Do not touch any other part of the codebase — no source files, no tests, no config — unless the user explicitly asks you to.
